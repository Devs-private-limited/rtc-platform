PORT=4000
JWT_SECRET=dev-secret-change-in-production
DEMO_APP_ID=demo-app
DEMO_APP_SECRET=demo-secret

# Optional — enables multi-instance signaling
REDIS_URL=redis://localhost:6379

# PostgreSQL app registry (Phase 3)
DATABASE_URL=postgresql://rtc:rtc@localhost:5432/rtc
ADMIN_API_KEY=dev-admin-key

# Optional — mediasoup SFU URL exposed to SDK clients
SFU_URL=http://localhost:4100

# Optional — OpenAI for transcription + AI summaries (Phase 5)
OPENAI_API_KEY=
# RECORDINGS_DIR=./data/recordings
TURN_URL=turn:localhost:3478
TURN_USERNAME=rtc
TURN_PASSWORD=rtc-turn-secret
